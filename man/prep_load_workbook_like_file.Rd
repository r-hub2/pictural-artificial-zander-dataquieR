% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_load_workbook_like_file.R
\name{prep_load_workbook_like_file}
\alias{prep_load_workbook_like_file}
\title{Pre-load a file with named (usually more than) one table(s)}
\usage{
prep_load_workbook_like_file(file, keep_types = FALSE, append = FALSE)
}
\arguments{
\item{file}{the file name to load.}

\item{keep_types}{\link{logical} keep types as possibly defined in the file.
set \code{TRUE} for study data.}

\item{append}{\link{logical} if a data frame already exists in the cache
(by name), extend the existing one}
}
\value{
\verb{invisible(the cache environment)}
}
\description{
These can thereafter be referred to by their names only. Such files are,
e.g., spreadsheet-workbooks or \code{RData}-files.
For workbook-like files, each loaded sheet is cached by its sheet name and
by qualified names that include the workbook base name, separated by \code{|}.
For example, sheet \code{tab1} in \code{meta_data_v2.xlsx} can be referred to as
\code{tab1}, \code{meta_data_v2.xlsx|tab1}, or \code{meta_data_v2|tab1} after the workbook
has been loaded. The short sheet name is convenient when the whole workbook is
loaded together. If item-level metadata are supplied without loading the full
metadata workbook, references in item-level columns such as \code{CODE_LIST_TABLE}
or \code{MISSING_LIST_TABLE} must use a name that is already present in the
data-frame cache, for example the qualified name \code{meta_data_v2.xlsx|tab1}.
}
\details{
Note, that this function in contrast to \link{prep_get_data_frame} does neither
support selecting specific sheets/columns from a file.
}
\seealso{
\link{prep_add_data_frames}

\link{prep_get_data_frame}

Other data-frame-cache:
\code{\link[=prep_add_data_frames]{prep_add_data_frames()}},
\code{\link[=prep_get_data_frame]{prep_get_data_frame()}},
\code{\link[=prep_list_dataframes]{prep_list_dataframes()}},
\code{\link[=prep_load_folder_with_metadata]{prep_load_folder_with_metadata()}},
\code{\link[=prep_purge_data_frame_cache]{prep_purge_data_frame_cache()}},
\code{\link[=prep_remove_from_cache]{prep_remove_from_cache()}}
}
\concept{data-frame-cache}
