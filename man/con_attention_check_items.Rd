% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/con_attention_check_items.R
\name{con_attention_check_items}
\alias{con_attention_check_items}
\title{Internal function only existing for technical reasons.}
\usage{
con_attention_check_items(
  resp_vars = NULL,
  study_data,
  label_col = VAR_NAMES,
  item_level = "item_level",
  meta_data = item_level,
  meta_data_v2,
  meta_data_cross_item = "cross-item_level",
  cross_item_level,
  `cross-item_level`
)
}
\arguments{
\item{resp_vars}{\link{variable} the names of the measurement variables, if
missing or \code{NULL}, all variables will be checked}

\item{study_data}{\link{data.frame} the data frame that contains the measurements}

\item{label_col}{\link{variable attribute} the name of the column in the metadata
with labels of variables}

\item{item_level}{\link{data.frame} the data frame that contains metadata
attributes of study data}

\item{meta_data}{\link{data.frame} old name for \code{item_level}}

\item{meta_data_v2}{\link{character} path to workbook like metadata file, see
\code{\link{prep_load_workbook_like_file}} for details.
\strong{ALL LOADED DATAFRAMES WILL BE PURGED},
using \code{\link{prep_purge_data_frame_cache}},
if you specify \code{meta_data_v2}.}

\item{meta_data_cross_item}{\link{data.frame} -- Cross-item level metadata}

\item{cross_item_level}{\link{data.frame} alias for \code{meta_data_cross_item}}

\item{`cross-item_level`}{\link{data.frame} alias for \code{meta_data_cross_item}}
}
\value{
a list with:
\itemize{
\item \code{SummaryData}: \link{data.frame} table with user friendly caption
\item \code{SummaryTable}: \link{data.frame} table with indicators
\item \code{FlaggedStudyData} \link{data.frame} contains the original data frame of the
variables used to calculate
the sum of attention check items
with an additional column indicating if
the
observational unit has a percentage of
incorrect bogus or instructed items
greater than the threshold.
}
}
\description{
Report the sum of attention check item mismatches.

\link{Indicator}
}
\details{
Planned to be removed in future releases.

\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\section{ALGORITHM OF THIS IMPLEMENTATION:}{
\itemize{
\item Implementation is restricted to attention check variables (e.g., bogus, or
instructed response items)
\item Remove missing codes from the study data (if defined in the metadata)
\item Consider missing values as non-matching expectations
\item The matrix is estimated for all variables of \code{resp_vars}
(variables of \code{ITEM_TYPE} BOGUS or INSTRUCTED, provided in the cross-item
level or obtained from item_level column \code{ITEM_TYPE})
\item Sum of non-matching values (1 if non-matching or missing variables, 0
otherwise)
\item By default is problematic if the percentage of attention check variables
non-matching the expected values is equal to or
greater than 50\% (\code{Curran} 2016)
}
}

