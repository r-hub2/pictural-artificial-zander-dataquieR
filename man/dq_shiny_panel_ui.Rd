% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dq_shiny_panel.R
\name{dq_shiny_panel_ui}
\alias{dq_shiny_panel_ui}
\alias{dq_shiny_panel_server}
\title{Embed dataquieR results in a Shiny application}
\usage{
dq_shiny_panel_ui(id, title = NULL, height = "700px")

dq_shiny_panel_server(id, result = NULL, report_dir = NULL, height = "700px")
}
\arguments{
\item{id}{\link{character} module id.}

\item{title}{\link{character} optional panel title.}

\item{height}{\link{character} CSS height of the embedded panel.}

\item{result}{optional computed \code{dataquieR_result} or \code{master_result}
object to display.}

\item{report_dir}{\link{character} optional directory created by \code{dq_report2()}
or \code{dq_report_by()}.}
}
\value{
\code{dq_shiny_panel_ui()} returns a Shiny UI tag list.

\code{dq_shiny_panel_server()} returns the Shiny module server value.
}
\description{
\code{dq_shiny_panel_ui()} and \code{dq_shiny_panel_server()} provide an experimental
Shiny module for showing already computed dataquieR output in an application.
The first version supports either a single result object or an existing
report directory. \code{shiny} remains an optional dependency.
}
\examples{
\dontrun{
if (requireNamespace("shiny", quietly = TRUE)) {
  ui <- shiny::fluidPage(
    dq_shiny_panel_ui("dq", title = "Data quality")
  )
  server <- function(input, output, session) {
    dq_shiny_panel_server("dq", report_dir = "my_report")
  }
  shiny::shinyApp(ui, server)
}
}

}
